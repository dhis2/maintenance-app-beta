
type DialogType = 'mangageViews' | 'bulkSharing' | 'download'

type ListDialogContext = {
    dialogOpen: DialogType | undefined
    setDialogType: (dialogType: DialogType) => void
}

const useListDialog = (dialogType: DialogType) => {
    con
}


export const ListDialogs = () => {
    