import { generateDefaultListTests } from '../defaultListTests'

generateDefaultListTests({ componentName: 'Indicators' })

xdescribe('Data sets additional tests', () => {
    it('should filter by indicator type', () => {})
})
